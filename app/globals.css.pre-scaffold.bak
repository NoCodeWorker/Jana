/* app/globals.css */
/* JANA OS Design Tokens */
/* Tailwind CSS v4 */

@import "tailwindcss";

@theme {
  /* Typography */
  --font-sans: "Inter", sans-serif;
  --font-display: "Outfit", sans-serif;

  /* Theme Mapping */
  --color-background: var(--background);
  --color-surface: var(--surface);
  --color-surface-elevated: var(--surface-elevated);
  --color-foreground: var(--foreground);
  --color-foreground-muted: var(--foreground-muted);
  --color-border: var(--border-color);

  --color-jana-primary: var(--jana-primary);
  --color-jana-primary-hover: var(--jana-primary-hover);
  --color-jana-primary-active: var(--jana-primary-active);
  --color-jana-primary-accessible: var(--jana-primary-accessible);

  --color-brain: var(--brain);
  --color-talent: var(--talent);
  --color-production: var(--production);

  --color-success: var(--success);
  --color-warning: var(--warning);
  --color-error: var(--error);
  --color-info: var(--info);

  /* Radius */
  --radius-sm: 10px;
  --radius-md: 12px;
  --radius-lg: 16px;
  --radius-xl: 20px;

  /* Shadows */
  --shadow-card: var(--shadow-card-val);

  /* Motion */
  --ease-standard: cubic-bezier(0.4, 0, 0.2, 1);
  --duration-fast: 150ms;
  --duration-normal: 250ms;
  --duration-slow: 400ms;
}

/* Default to dark mode (Backstage default) */
:root {
  color-scheme: dark;
  
  --background: #121417;
  --surface: #1A1E24;
  --surface-elevated: #232A33;
  --foreground: #F5F7FA;
  --foreground-muted: #9EADB0;
  
  --jana-primary: #ec690c;
  --jana-primary-hover: #f07e24;
  --jana-primary-active: #d15c0a;
  --jana-primary-accessible: #f28533;

  --brain: #7C5CFF;
  --talent: #1FBF75;
  --production: #F5B74F;

  --success: #1FBF75;
  --warning: #F5B74F;
  --error: #E5484D;
  --info: #4C8DFF;
  
  --border-color: rgba(255, 255, 255, 0.08);
  --shadow-card-val: 0 4px 12px rgba(0, 0, 0, 0.3);
}

/* Light mode overrides */
:root.light {
  color-scheme: light;
  
  --background: #F5F7FA;
  --surface: #FFFFFF;
  --surface-elevated: #EAEFF5;
  --foreground: #121417;
  --foreground-muted: #5C6A79;

  --jana-primary: #ec690c;
  --jana-primary-hover: #d15c0a;
  --jana-primary-active: #b54f08;
  --jana-primary-accessible: #ec690c;

  --brain: #643DFF;
  --talent: #189e60;
  --production: #d9992b;

  --success: #189e60;
  --warning: #d9992b;
  --error: #c92a2a;
  --info: #1c7ed6;

  --border-color: rgba(0, 0, 0, 0.08);
  --shadow-card-val: 0 4px 12px rgba(0, 0, 0, 0.06);
}

/* Explicit dark mode class overrides */
:root.dark {
  color-scheme: dark;
  
  --background: #121417;
  --surface: #1A1E24;
  --surface-elevated: #232A33;
  --foreground: #F5F7FA;
  --foreground-muted: #9EADB0;
  
  --jana-primary: #ec690c;
  --jana-primary-hover: #f07e24;
  --jana-primary-active: #d15c0a;
  --jana-primary-accessible: #f28533;

  --brain: #7C5CFF;
  --talent: #1FBF75;
  --production: #F5B74F;

  --success: #1FBF75;
  --warning: #F5B74F;
  --error: #E5484D;
  --info: #4C8DFF;
  
  --border-color: rgba(255, 255, 255, 0.08);
  --shadow-card-val: 0 4px 12px rgba(0, 0, 0, 0.3);
}

html {
  scroll-behavior: smooth;
}

body {
  background-color: var(--background);
  color: var(--foreground);
  font-family: var(--font-sans);
  transition: background-color var(--duration-normal) var(--ease-standard), color var(--duration-normal) var(--ease-standard);
}

h1, h2, h3, h4, h5, h6 {
  font-family: var(--font-display);
}

.glass-panel {
  background: rgba(255, 255, 255, 0.04);
  backdrop-filter: blur(16px);
  border: 1px solid rgba(255, 255, 255, 0.08);
}

:root.light .glass-panel {
  background: rgba(0, 0, 0, 0.02);
  backdrop-filter: blur(16px);
  border: 1px solid rgba(0, 0, 0, 0.06);
}

/* Link Wrapped Images Effect */
a img,
a picture img {
  filter: grayscale(100%);
  transition: filter var(--duration-normal) var(--ease-standard);
}

a:hover img,
a:focus img,
a:focus-visible img {
  filter: grayscale(0%);
}

@media (prefers-reduced-motion: reduce) {
  *,
  *::before,
  *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
    scroll-behavior: auto !important;
  }
}
